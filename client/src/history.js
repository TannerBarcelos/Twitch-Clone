import {createBrowserHistory} from 'history'; //to create a new history object such that we can maintain and give the browser router access to
export default createBrowserHistory ();

// this is all we need to set up browser history and confiure a history object to auto-navigate to any route in our route spec.
// after some logic
